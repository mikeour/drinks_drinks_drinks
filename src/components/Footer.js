import React from "react";

/** @jsx jsx */
import { css, jsx } from "@emotion/core";

export default () => (
  <footer
    css={css`
      grid-area: footer;
      border-top: 1px solid black;
    `}
  >
    <h1 />
  </footer>
);

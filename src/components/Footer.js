import React from "react";

/** @jsx jsx */
import { css, jsx } from "@emotion/core";

export default () => (
  <footer
    css={css`
      grid-area: footer;
    `}
  >
    <h1>Footer</h1>
  </footer>
);
